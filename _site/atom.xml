<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
 
 <title>JxmBar Blog</title>
 <link href="http://jxm080511321.github.io/atom.xml" rel="self"/>
 <link href="http://jxm080511321.github.io"/>
 <updated>2016-10-09T14:11:14+08:00</updated>
 <author>
   <name></name>
   <email></email>
 </author>

 
 <entry>
   <title>腾讯定位SDK FAQ</title>
   <link href="http://jxm080511321.github.io/faq/tencent-location-sdk-faq.html"/>
   <updated>2015-01-22T00:00:00+08:00</updated>
   <id>/faq/tencent-location-sdk-faq</id>
   <content type="html">&lt;p&gt;本文总结了腾讯定位SDK常见的使用问题&lt;/p&gt;

&lt;h1 id=&quot;section&quot;&gt;1. 遇到问题&lt;/h1&gt;
&lt;h2 id=&quot;section-1&quot;&gt;1.1 使用问题&lt;/h2&gt;
&lt;ol&gt;
  &lt;li&gt;首先确认Demo有没同样问题？没有的话，Demo是怎么写的，跟您的代码有什么不同？&lt;/li&gt;
  &lt;li&gt;看看&lt;a href=&quot;http://open.map.qq.com/geosdk/doc/&quot;&gt;参考文档&lt;/a&gt; (文档表述得不清楚的位置也欢迎向我 qq 410063005 提出)&lt;/li&gt;
  &lt;li&gt;QQ群提问或 在&lt;a href=&quot;http://bbs.map.qq.com/forum-47-1.html&quot;&gt;官方论坛&lt;/a&gt; Android SDK 版块发贴求助&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;bug&quot;&gt;1.2 疑似bug&lt;/h2&gt;
&lt;p&gt;如果遇到定位SDK引起app崩溃，或产生一些跟预期完全不符的行为怀疑是bug，请向我们(QQ 86147470、410063005)发送 &lt;font color=&quot;red&quot;&gt;问题现象描述 + 错误日志&lt;/font&gt;，并且告知&lt;font color=&quot;red&quot;&gt;您的调用方式或发送代码片断&lt;/font&gt;&lt;/p&gt;

&lt;h1 id=&quot;faq&quot;&gt;2. FAQ&lt;/h1&gt;
&lt;h2 id=&quot;section-2&quot;&gt;我要混淆代码，怎么配置?&lt;/h2&gt;
&lt;p&gt;注意不要混淆 native 方法&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;-keepclasseswithmembernames class * {
    native &amp;lt;methods&amp;gt;;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;如果使用的 Android SDK版本低于18，混淆时可能出现  &lt;code class=&quot;highlighter-rouge&quot;&gt;android.location.Location&lt;/code&gt; 相关的错误，添加以下proguard配置：&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;-dontwarn android.location.Location
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;定位SDK从4.0版本开始还需要添加以下配置&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;-dontwarn  org.eclipse.jdt.annotation.**
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;定位SDK从4.1版本开始还需要添加以下配置&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;-keepclassmembers class ** {
	public void on*Event(...);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;完整的配置如下：&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;-keepclassmembers class ** {
    public void on*Event(...);
}
-keepclasseswithmembernames class * {
  native &amp;lt;methods&amp;gt;;
}
-dontwarn  org.eclipse.jdt.annotation.**
-dontwarn android.location.Location
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&quot;gps&quot;&gt;GPS定不了位？&lt;/h2&gt;
&lt;p&gt;关于GPS应有以下几个基本认识：&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;GPS只能得到坐标值，需要通过网络才能根据坐标值得到地址信息&lt;/li&gt;
  &lt;li&gt;GPS首次定位非常可能非常耗时，不同机器上差异较大&lt;/li&gt;
  &lt;li&gt;GPS在信号好的情况下才能定位，一般要求在室外、无遮挡；一些机器在窗户边有可能可以成功定位。&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;section-3&quot;&gt;定位误差有时上千米，正常吗？&lt;/h2&gt;
&lt;p&gt;定位存在精度问题，精度由高到低如下：&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;GPS &amp;gt; Wi-Fi &amp;gt; 基站
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;在不开 GPS 和 Wi-Fi 的情况下，如果遇到覆盖范围相当大的基站，定位精度将非常低。&lt;/p&gt;

&lt;p&gt;注：&lt;font color=&quot;red&quot;&gt;开 Wi-Fi 指打开 Wi-Fi 开关即可，并不要求一定连接到某个热点&lt;/font&gt;。如果您的 app 对精度要求较高，建议 app 提示用户打开 Wi-Fi 以提高定位精度(类似于地图类 app 提示用户打开 GPS)。&lt;/p&gt;

&lt;h2 id=&quot;section-4&quot;&gt;为什么模拟器上无法定位?&lt;/h2&gt;
&lt;p&gt;模拟器不能得到基站和Wi-Fi热点信息，所以不能定位。&lt;font color=&quot;red&quot;&gt;请在真实设备上使用定位SDK进行开发&lt;/font&gt;。&lt;/p&gt;

&lt;h2 id=&quot;gps-1&quot;&gt;怎样区分是”GPS定位”还是”网络定位”?&lt;/h2&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;TencentLocation location = ...;
String provider = location.getProvider();
if (TencentLocation.GPS_PROVIDER.equals(provider)) {
	// location 是GPS定位结果
} else if (TencentLocation.NETWORK_PROVIDER.equals(provider)) {
	// location 是网络定位结果
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&quot;tencentlocationgetxxx&quot;&gt;TencentLocation.getXXX()返回值为空？&lt;/h2&gt;
&lt;p&gt;比如, 将 &lt;code class=&quot;highlighter-rouge&quot;&gt;getCity()&lt;/code&gt; 的返回值打印出来发现为空。&lt;/p&gt;

&lt;p&gt;由于使用场景的差异非常大(有些app只需将坐标点显示在底图上，有些app需要街道地区，有些app需要知道行政区划，有些app需要附近的POI，等等)，定位请求有不同的 &lt;code class=&quot;highlighter-rouge&quot;&gt;Request Level&lt;/code&gt;。&lt;/p&gt;

&lt;font color=&quot;red&quot;&gt;不同的 Level 的 TencentLocationRequest 请求得到的 TencentLocation 的完整程度不同&lt;/font&gt;
&lt;p&gt;。定位前应当设置正确的 &lt;code class=&quot;highlighter-rouge&quot;&gt;Request Level&lt;/code&gt;。&lt;/p&gt;

&lt;h2 id=&quot;section-5&quot;&gt;在新的线程中定位?&lt;/h2&gt;
&lt;p&gt;完全没有必要。&lt;font color=&quot;red&quot;&gt;建议直接在主线程中调用 `TencentLocationManager.requestLocationUpdates()` 来定位&lt;/font&gt;。&lt;/p&gt;

&lt;p&gt;原因如下：&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;定位SDK内部有相应的工作线程用于处理网络请求/响应过程，不会阻塞主线程&lt;/li&gt;
  &lt;li&gt;在主线程发起定位， &lt;code class=&quot;highlighter-rouge&quot;&gt;TencentLocationListener.onLocationChanged()&lt;/code&gt; 是在主线程中回调，可直接操作 UI&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;section-6&quot;&gt;一定要在新的线程中定位，如何实现?&lt;/h2&gt;
&lt;p&gt;如上所说，没必要在新的线程中定位。使用如下方式可以将定位SDK内部操作放到新的线程中，一定程度上减少主线程负担：&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;TencentLocationManager locationManager = ...
// 创建并运行 HandlerThread
HandlerThread thread = new HandlerThread();
thread.start();
// 在主线程发起定位
locationManager.requestLocationUpdates(request, listener, thread.getLooper());
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&quot;requestlocationupdates-&quot;&gt;可否连续调用 requestLocationUpdates ()?&lt;/h2&gt;
&lt;p&gt;目前只支持一个 TencentLocationListener。连续调用后，将自动取消原来的 listener，并使用新的 request 和 listener 定位。&lt;/p&gt;

&lt;p&gt;可以利用这个特点来调整定位频率而不必 &lt;code class=&quot;highlighter-rouge&quot;&gt;removeUpdates()&lt;/code&gt; 后再 &lt;code class=&quot;highlighter-rouge&quot;&gt;requestLocationUpdates()&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;定位完成后，只需要调用 &lt;strong&gt;一次&lt;/strong&gt; &lt;code class=&quot;highlighter-rouge&quot;&gt;removeUpdates()&lt;/code&gt; 来取消定位。&lt;/p&gt;

&lt;h2 id=&quot;so&quot;&gt;so库引起的崩溃，怎么办？&lt;/h2&gt;
&lt;p&gt;建议通过下面步骤排查：&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;检查设备的 CPU 架构&lt;/li&gt;
  &lt;li&gt;检查是否将 so 文件拷贝到项目中相应的目录&lt;/li&gt;
  &lt;li&gt;检查最终生成的 apk 中是否包含相应的 so 文件&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;gps-2&quot;&gt;模拟GPS定位不起作用&lt;/h2&gt;
&lt;p&gt;定位SDK使用一些策略过滤掉模拟的GPS位置，建议使用真实GPS位置测试。&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>腾讯定位SDK FAQ</title>
   <link href="http://jxm080511321.github.io/faq/tencent-location-sdk-faq-%E5%89%AF%E6%9C%AC.html"/>
   <updated>2015-01-22T00:00:00+08:00</updated>
   <id>/faq/tencent-location-sdk-faq - 副本</id>
   <content type="html">&lt;p&gt;本文总结了腾讯定位SDK常见的使用问题&lt;/p&gt;

&lt;h1 id=&quot;section&quot;&gt;1. 遇到问题&lt;/h1&gt;
&lt;h2 id=&quot;section-1&quot;&gt;1.1 使用问题&lt;/h2&gt;
&lt;ol&gt;
  &lt;li&gt;首先确认Demo有没同样问题？没有的话，Demo是怎么写的，跟您的代码有什么不同？&lt;/li&gt;
  &lt;li&gt;看看&lt;a href=&quot;http://open.map.qq.com/geosdk/doc/&quot;&gt;参考文档&lt;/a&gt; (文档表述得不清楚的位置也欢迎向我 qq 410063005 提出)&lt;/li&gt;
  &lt;li&gt;QQ群提问或 在&lt;a href=&quot;http://bbs.map.qq.com/forum-47-1.html&quot;&gt;官方论坛&lt;/a&gt; Android SDK 版块发贴求助&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;bug&quot;&gt;1.2 疑似bug&lt;/h2&gt;
&lt;p&gt;如果遇到定位SDK引起app崩溃，或产生一些跟预期完全不符的行为怀疑是bug，请向我们(QQ 86147470、410063005)发送 &lt;font color=&quot;red&quot;&gt;问题现象描述 + 错误日志&lt;/font&gt;，并且告知&lt;font color=&quot;red&quot;&gt;您的调用方式或发送代码片断&lt;/font&gt;&lt;/p&gt;

&lt;h1 id=&quot;faq&quot;&gt;2. FAQ&lt;/h1&gt;
&lt;h2 id=&quot;section-2&quot;&gt;我要混淆代码，怎么配置?&lt;/h2&gt;
&lt;p&gt;注意不要混淆 native 方法&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;-keepclasseswithmembernames class * {
    native &amp;lt;methods&amp;gt;;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;如果使用的 Android SDK版本低于18，混淆时可能出现  &lt;code class=&quot;highlighter-rouge&quot;&gt;android.location.Location&lt;/code&gt; 相关的错误，添加以下proguard配置：&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;-dontwarn android.location.Location
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;定位SDK从4.0版本开始还需要添加以下配置&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;-dontwarn  org.eclipse.jdt.annotation.**
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;定位SDK从4.1版本开始还需要添加以下配置&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;-keepclassmembers class ** {
	public void on*Event(...);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;完整的配置如下：&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;-keepclassmembers class ** {
    public void on*Event(...);
}
-keepclasseswithmembernames class * {
  native &amp;lt;methods&amp;gt;;
}
-dontwarn  org.eclipse.jdt.annotation.**
-dontwarn android.location.Location
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&quot;gps&quot;&gt;GPS定不了位？&lt;/h2&gt;
&lt;p&gt;关于GPS应有以下几个基本认识：&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;GPS只能得到坐标值，需要通过网络才能根据坐标值得到地址信息&lt;/li&gt;
  &lt;li&gt;GPS首次定位非常可能非常耗时，不同机器上差异较大&lt;/li&gt;
  &lt;li&gt;GPS在信号好的情况下才能定位，一般要求在室外、无遮挡；一些机器在窗户边有可能可以成功定位。&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;section-3&quot;&gt;定位误差有时上千米，正常吗？&lt;/h2&gt;
&lt;p&gt;定位存在精度问题，精度由高到低如下：&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;GPS &amp;gt; Wi-Fi &amp;gt; 基站
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;p&gt;在不开 GPS 和 Wi-Fi 的情况下，如果遇到覆盖范围相当大的基站，定位精度将非常低。&lt;/p&gt;

&lt;p&gt;注：&lt;font color=&quot;red&quot;&gt;开 Wi-Fi 指打开 Wi-Fi 开关即可，并不要求一定连接到某个热点&lt;/font&gt;。如果您的 app 对精度要求较高，建议 app 提示用户打开 Wi-Fi 以提高定位精度(类似于地图类 app 提示用户打开 GPS)。&lt;/p&gt;

&lt;h2 id=&quot;section-4&quot;&gt;为什么模拟器上无法定位?&lt;/h2&gt;
&lt;p&gt;模拟器不能得到基站和Wi-Fi热点信息，所以不能定位。&lt;font color=&quot;red&quot;&gt;请在真实设备上使用定位SDK进行开发&lt;/font&gt;。&lt;/p&gt;

&lt;h2 id=&quot;gps-1&quot;&gt;怎样区分是”GPS定位”还是”网络定位”?&lt;/h2&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;TencentLocation location = ...;
String provider = location.getProvider();
if (TencentLocation.GPS_PROVIDER.equals(provider)) {
	// location 是GPS定位结果
} else if (TencentLocation.NETWORK_PROVIDER.equals(provider)) {
	// location 是网络定位结果
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&quot;tencentlocationgetxxx&quot;&gt;TencentLocation.getXXX()返回值为空？&lt;/h2&gt;
&lt;p&gt;比如, 将 &lt;code class=&quot;highlighter-rouge&quot;&gt;getCity()&lt;/code&gt; 的返回值打印出来发现为空。&lt;/p&gt;

&lt;p&gt;由于使用场景的差异非常大(有些app只需将坐标点显示在底图上，有些app需要街道地区，有些app需要知道行政区划，有些app需要附近的POI，等等)，定位请求有不同的 &lt;code class=&quot;highlighter-rouge&quot;&gt;Request Level&lt;/code&gt;。&lt;/p&gt;

&lt;font color=&quot;red&quot;&gt;不同的 Level 的 TencentLocationRequest 请求得到的 TencentLocation 的完整程度不同&lt;/font&gt;
&lt;p&gt;。定位前应当设置正确的 &lt;code class=&quot;highlighter-rouge&quot;&gt;Request Level&lt;/code&gt;。&lt;/p&gt;

&lt;h2 id=&quot;section-5&quot;&gt;在新的线程中定位?&lt;/h2&gt;
&lt;p&gt;完全没有必要。&lt;font color=&quot;red&quot;&gt;建议直接在主线程中调用 `TencentLocationManager.requestLocationUpdates()` 来定位&lt;/font&gt;。&lt;/p&gt;

&lt;p&gt;原因如下：&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;定位SDK内部有相应的工作线程用于处理网络请求/响应过程，不会阻塞主线程&lt;/li&gt;
  &lt;li&gt;在主线程发起定位， &lt;code class=&quot;highlighter-rouge&quot;&gt;TencentLocationListener.onLocationChanged()&lt;/code&gt; 是在主线程中回调，可直接操作 UI&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;section-6&quot;&gt;一定要在新的线程中定位，如何实现?&lt;/h2&gt;
&lt;p&gt;如上所说，没必要在新的线程中定位。使用如下方式可以将定位SDK内部操作放到新的线程中，一定程度上减少主线程负担：&lt;/p&gt;

&lt;div class=&quot;highlighter-rouge&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;TencentLocationManager locationManager = ...
// 创建并运行 HandlerThread
HandlerThread thread = new HandlerThread();
thread.start();
// 在主线程发起定位
locationManager.requestLocationUpdates(request, listener, thread.getLooper());
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

&lt;h2 id=&quot;requestlocationupdates-&quot;&gt;可否连续调用 requestLocationUpdates ()?&lt;/h2&gt;
&lt;p&gt;目前只支持一个 TencentLocationListener。连续调用后，将自动取消原来的 listener，并使用新的 request 和 listener 定位。&lt;/p&gt;

&lt;p&gt;可以利用这个特点来调整定位频率而不必 &lt;code class=&quot;highlighter-rouge&quot;&gt;removeUpdates()&lt;/code&gt; 后再 &lt;code class=&quot;highlighter-rouge&quot;&gt;requestLocationUpdates()&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;定位完成后，只需要调用 &lt;strong&gt;一次&lt;/strong&gt; &lt;code class=&quot;highlighter-rouge&quot;&gt;removeUpdates()&lt;/code&gt; 来取消定位。&lt;/p&gt;

&lt;h2 id=&quot;so&quot;&gt;so库引起的崩溃，怎么办？&lt;/h2&gt;
&lt;p&gt;建议通过下面步骤排查：&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;检查设备的 CPU 架构&lt;/li&gt;
  &lt;li&gt;检查是否将 so 文件拷贝到项目中相应的目录&lt;/li&gt;
  &lt;li&gt;检查最终生成的 apk 中是否包含相应的 so 文件&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;gps-2&quot;&gt;模拟GPS定位不起作用&lt;/h2&gt;
&lt;p&gt;定位SDK使用一些策略过滤掉模拟的GPS位置，建议使用真实GPS位置测试。&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>腾讯定位SDK开发指南</title>
   <link href="http://jxm080511321.github.io/tutorial/tencent-location-sdk-tutorial.html"/>
   <updated>2014-11-21T00:00:00+08:00</updated>
   <id>/tutorial/tencent-location-sdk-tutorial</id>
   <content type="html">&lt;h1 id=&quot;tutorialtencent-location-sdk-tutorial-01html&quot;&gt;&lt;a href=&quot;/tutorial/tencent-location-sdk-tutorial-01.html&quot;&gt;1. 入门&lt;/a&gt;&lt;/h1&gt;
&lt;h1 id=&quot;tutorialtencent-location-sdk-tutorial-02html&quot;&gt;&lt;a href=&quot;/tutorial/tencent-location-sdk-tutorial-02.html&quot;&gt;2. 拓展&lt;/a&gt;&lt;/h1&gt;
</content>
 </entry>
 
 <entry>
   <title>腾讯定位SDK开发指南-扩展</title>
   <link href="http://jxm080511321.github.io/tutorial/tencent-location-sdk-tutorial-02.html"/>
   <updated>2014-11-21T00:00:00+08:00</updated>
   <id>/tutorial/tencent-location-sdk-tutorial-02</id>
   <content type="html">&lt;p&gt;注意：继续阅读本节前请确保&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;您已了解定位SDK的&lt;a href=&quot;/tutorial/tencent-location-sdk-tutorial-01.html&quot;&gt;基本用法&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;您是熟练的Android开发者&lt;/li&gt;
&lt;/ol&gt;
</content>
 </entry>
 
 <entry>
   <title>腾讯定位SDK开发指南-入门</title>
   <link href="http://jxm080511321.github.io/tutorial/tencent-location-sdk-tutorial-01.html"/>
   <updated>2014-11-21T00:00:00+08:00</updated>
   <id>/tutorial/tencent-location-sdk-tutorial-01</id>
   <content type="html">&lt;h1 id=&quot;section&quot;&gt;1. 简介&lt;/h1&gt;

&lt;h1 id=&quot;section-1&quot;&gt;2. 安装&lt;/h1&gt;

&lt;h1 id=&quot;section-2&quot;&gt;3. 使用&lt;/h1&gt;

&lt;h1 id=&quot;section-3&quot;&gt;4. 示例&lt;/h1&gt;

&lt;h1 id=&quot;section-4&quot;&gt;5. 注意事项&lt;/h1&gt;
</content>
 </entry>
 
 
</feed>